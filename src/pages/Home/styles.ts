import styled from 'styled-components';

export const Container = styled.div`
    color: var(--black);
    padding: 1.5rem;
    width: 100%; 
    height: 100vh;
    overflow: scroll;
    overflow-x: hidden;
`;